# Base URL the browser will use to reach the .NET API.
# Astro only exposes PUBLIC_-prefixed vars to client bundles, and since
# this is a static build, whatever you set here gets baked into the
# compiled JS at `npm run build` time — build once per target server.
PUBLIC_API_BASE_URL=https://192.168.1.50:7246
